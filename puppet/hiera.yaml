# Puppet Hiera Data
:yaml:
  :datadir: .
:hierarchy:
  - "puppet/%{environment}"
  - puppet/hiera
  - scm_creds
  - vagrant-nsidc

classes:
  - nsidc_nfs

nsidc_nfs::sharemounts:
  /share/apps/icebridge-portal:
    project: apps
    share: icebridge-portal
