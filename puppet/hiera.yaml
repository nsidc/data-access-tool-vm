# Puppet Hiera Data
:yaml:
  :datadir: .
:hierarchy:
  - "puppet/%{environment}"
  - puppet/hiera
  - scm_creds
  - vagrant-nsidc

classes:
  - nsidc_nfs

nsidc_nfs::sharemounts:
  /export/apps/live/integration:
    project: apps
    share: live/integration
